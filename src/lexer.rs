use std::collections::HashMap;

use crate::token::tokens::Token;

pub mod lexer {
    pub struct Lexer;

    impl Lexer {
        pub fn new() -> Self {
            Self
        }

        pub fn lex(s: &str) {
            let split = s.split();
            
        }
    }
}
